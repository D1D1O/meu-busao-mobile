import React from 'react';
import { View } from 'react-native';

import {
	Container
} from './styles';

const Routes: React.FC = () => {
	return (
		<Container>

		</Container>
	);
}

export default Routes;